# 変更履歴

## 計画

- 可変長配列の実装
- 構造体の実装
- 所有権システムもしくはガベージコレクタの実装

## リリース済み

### jsonpiler 0.9

#### 0.9.3

- 変更
  - 関数が使用されたかどうかを管理する機構を改善

#### 0.9.2

- 変更
  - バージョン情報を`Cargo.toml`から自動生成するようになった

#### 0.9.1

- 修正
  - `Str`に対する`!=`が正しく機能しない問題

#### 0.9.0

- 追加
  - コマンド: `format`, `server`, `release`
  - 新しい関数: `export`, `confirm`, `main`, `<<`, `>>`, `slice`
  - `Str`における`==`と`!=`を追加
  - フォーマット機能とエラーを診断するLSPサーバー機能
  - `let`(variable = value) によるローカル変数定義
  - `global`(variable = value) によるグローバル変数定義
  - 比較関数が`Float`に対応
  - 演算子優先順位
  - 拡張機能内の JSPL 言語サーバー
  - 拡張機能内の `Run JSPL`

- 変更
  - `include` -> `import`
  - 関数名と変数名の衝突が許されなくなった
  - `import`には対象ファイル内で`export`が必要になった
  - 変数の定義と再代入の構文を分離
  - `=` を再代入専用に
  - 1つのみの`if([cond, any])`の`[]`を省略できるように
  - `concat`がリテラル以外も結合するように
  - 予期しないメモリリークを実行時に検知するように
    (メモリリークは仕様として想定されていない)
  - `import`で他のファイルを読み込む際、初回読み込み時ではなく、
    スタートアップ時に実行されるように
  - 未使用の変数、引数、関数に警告を出すように
    この警告は、変数の先頭に`_`をつけることで回避できる
  - `len`が文字列のバイト長ではなく文字の数を表すように。
  - `GUI`によって呼ばれる関数名がウィンドウのタイトルバーに表示されるように
  - 使用したユーザー定義関数とその内部で使用する関数のみをリンクするように
  - 非リリースビルド時に算術演算のオーバーフローに対してエラーを出すように
  - JSPLの引数と`Array`が末尾のトレーリングカンマを許すように
  - 空のJSPLファイルが`null`を意味するようになった

- 削除
  - JSPLの変数先頭の`$`
  - cargo doc
  - 関数: `'`, `eval`

### jsonpiler 0.8

#### 0.8.0

- 追加
  - コマンド: `help`, `version`, ` `, `release`, `build`, `build release`
  - `release`版では、.exe内のデバッグ情報が取り除かれる。
  - ヒープメモリ管理機能
  - 新しい関数: `sqrt`, `input`, `Str`
  - 複合代入演算子: `+=`, `-=`, `*=`, `/=`
  - `error_cases`フォルダ
  - 実行時エラー内のエラー位置などの情報

- 変更
  - `String`型 -> `Str`型
  - 生成された.exeがJsonpilerのバージョン情報を含むように
  - `GUI`の終了後、新しい`GUI`を生成できるように
  - `GUI`のウィンドウ外にマウスがある場合の位置を正しく取得するように
  - `GUI`がPCのスリープ時に強制終了しないように
  - ~~`GUI`が存在するファイル名が自身のタイトルバーに表示されるように~~
  - Ctrl+Cやシャットダウン時にエラーメッセージを出すように

### jsonpiler 0.7

#### 0.7.4

- 変更
  - エラーチェックおよびエラー文生成の改良

#### 0.7.3

- 追加
  - SEH (構造化例外処理)

- 変更
  - コンパイルエラーの伝播にEnumを用いて体系化
  - ~~実行時のゼロ除算エラーをSEHに譲渡~~
  - 算術演算のコンパイル時の第二引数以降のどれかが0の場合最適化
  - 空のオブジェクトが評価された場合、`Null`を返すように

- 修正
  - globalの再代入のバグ

- 削除
  - ユーザー定義関数の引数の数の上限

#### 0.7.2

- 追加
  - 関数の途中リターン機構: `ret`
  - ループ制御機構: `break`, `continue`

- 変更
  - 整数格納やローカルメモリ参照の最適化により、生成 .exe の約3割の機械語部を削減

#### 0.7.1

- 追加
  - `not`,`and`,`or`,`xor` に対して `Int` ビット演算をサポート

- 変更
  - `Int`の算術演算子 (`+`,`-`,`*`,`/`,`%`) に定数畳み込み最適化を行うように
  - 文字列リテラルを.rdataセクションに置くように
  - アセンブリ中間表現のメモリ効率と構造

#### 0.7.0

- 追加
  - GUI機能: `GUI`
  - jsplが新しい記法をサポート: `1 + 10 + 1` が `+(1, 10, 1)` と同じに解釈されるようになった
  - `GUI`関数を使用し生成したexeを実行するためには`gdi32.dll`が必要(64bit Windowsには標準搭載)

- 変更
  - `global`が再代入可能に。
  - `global`がスレッドセーフに
  - jsplで同じ行に複数回関数呼び出しなどを書くとき、セミコロンが必要に

### jsonpiler 0.6

#### 0.6.5

- 追加
  - 関数: `assert`, `random`
  - 比較関数: `>`, `>`, `!=`
  - モジュールシステム: `include`
  - `abs`関数が`Float`型に対応。

- 変更
  - Int型が最小値(0xffffffffffffffff)を正しく扱えるように
  - エラーメッセージのフォーマット
  - `print`関数がパイプ、リダイレクトに対応。

- 修正
  - `Float`型が`-`関数に渡されたとき正しく符号反転されない問題

#### 0.6.4

- 修正
  - ユーザー定義関数の第五引数以降が正しく認識されない問題

#### 0.6.3

- 追加
  - 四則演算`+`, `-`, `*`, `/`がFloat型をサポート
  - Float型を整数に切り捨てる関数を追加: `Int`
  - I/O関数を追加: `print`

- 修正
  - global関数のエラー

#### 0.6.2

- 追加
  - 新しい関数: `len`

- 変更
  - ~~ユーザー定義関数の最大引数を16に~~
  - Jsonpilerが正しいexeの終了コードを返すように

- 修正
  - ユーザー定義関数にbool値が正しく渡されていない問題
  - ローカル変数に格納された文字列が正しく認識されない問題

#### 0.6.1

- 追加
  - JSPL
    JSPL (Jsonpiler Structured Programming Language)は、
    人間にとって書きやすく読みやすい記述を可能にするために導入された、
    Jsonpiler の新しい構文です
  - 言語仕様の日本語版

#### 0.6.0

- 追加
  - ループ構造: `while`
  - 比較関数: `==`, `<`, `<=`
  - 関数の登録や、再帰呼び出しを備えた`define`

- 変更
  - ローカル変数への再代入がほとんどの型で使用可能に

- 修正
  - アセンブラの組み込みにより発生したエスケープ文字が正しく表示されない問題

- 削除
  - `Function`型
  - 既存の`lambda`

### jsonpiler 0.5

#### 0.5.3 ~ 0.5.4

- 修正
  - 0.5.0 の重大なバグ

#### 0.5.1 ~ 0.5.2

- 修正
  .gitattributes に\*.bin binaryを追加していない問題

#### 0.5.0

- 追加
  - jsonpiler 内の自家製アセンブラ兼リンカ

- 修正
  - 一部の mov 命令に 32bit 以上の値を入れるとアセンブラがエラーを起こす問題

- 削除
  - .s ファイルの生成
  - .obj ファイルの生成
  - GNU asへの依存
  - GNU ldへの依存
  - ~~SEH (一時的)~~

### jsonpiler 0.4

#### 0.4.2

- 追加
  - 新しい関数: `concat`
  - `Object` を 3 つの亜種に分割
    - **HashMap**: キーと値のペアのコレクションを表す
    - ~~Sequence~~**Block**: 命令の順序付けられたシーケンスを表す
    - **TypeAnnotations**: 変数または関数の型アノテーションを表す
  - `lambda`の引数

- 変更
  - `lambda`の返り値にできる型が豊富に
  - 四則演算関数の引数を与えない場合、エラーが出るように
  - `+`, `/`, `*`, `or`, `and`, `xor`の最低限の引数の数が 2 に
- `message`の返り値が`Null`に

#### 0.4.1

- 追加
  - `Block`の返り値を追加するために使用される関数: `value`

- 変更
  - アセンブリ命令の保存方法
  - 組み込み関数のドキュメントが肥大化したため、複数のファイルに分割した
  - 変数に束縛されていない関数の引数として渡された一時的な値が解放されるタイミングが、
    その関数が終了したときに定まった。(例外として、`if`の本体の最後の一時的な値は解放される)
  - `if`, `scope`,`lambda`の引数の形式が変更された

- 削除
  - c の関数(malloc, free)への依存をなくしたことにより、`ucrtbase.dll`が不要となった
  - 入力ファイルを 1GB 以下に制限したため、不要となった安全性チェック

#### 0.4.0

- 追加
  - 論理演算関数: `not`, `xor`, `or`, `and`

- 変更
  - 組み込み関数が肥大化したため、複数のファイルに分割
  - 命令の肥大化による性能低下が予想されるため、bool 型のメモリ領域を 1bit から 1byte に変更
  - 関数の引数検証を直感的に

### jsonpiler 0.3

#### 0.3.5

- 追加
  - CHANGELOG と README の日本語版
  - `if` 関数

- 変更
  - ~~生成されたアセンブリの命令列周りの空白文字をタブに~~

- 削除
  - 内容が希薄なドキュメンテーションコメント
  - 0.1.6 ドキュメントバグ修正時に不要となった代替ドキュメント

#### 0.3.4

- 追加
  - `bool` 型の代入をサポート

- 変更
  - より保守しやすくアセンブリ命令とラベルを生成する体系的なシステム

#### 0.3.3

- 修正
  - スコープベースのロジックを処理する際にスタックからメモリが正しく解放されないバグ
  - `scope`関数が第一引数を無視するバグ

- 削除
  - 冗長な安全性チェック

#### 0.3.2

- 追加
  - `Float`型の代入をサポート

- 変更
  - 関数定義がグローバルスコープのみを明示的に共有するようになりました

- 修正
  - `global`関数のバグ

#### 0.3.1

- 追加
  - 新しい関数 `/`, `abs`, `%`

#### 0.3.0

- 変更
  - Json オブジェクトがキーの重複を許容するようになりました。
  - オブジェクトは関数呼び出しとして扱われるようになりました:  
    JSON オブジェクトの各キーは関数名として解釈され、対応する値は関数の引数として扱われます
  - すでに組み込み関数として存在する変数名に、ユーザー定義関数を代入することはできなくなりました
  - 配列は全ての要素の評価結果を残すようになりました
  - 複数のキー関数のエントリーをサポート:  
    オブジェクトに複数のキーが含まれる場合、それぞれが順番に評価され、最後の関数の結果が返されます
  - 引数が配列でない場合、角括弧を省略できるようになりました

- 削除
  - `begin`関数

### jsonpiler 0.2

#### 0.2.3

- 追加
  - 効率的なスタック解放を可能にするための一時的な値のトラッキング
- 変更
  - ~~オブジェクト・キーの重複が許されなくなりました~~
  - `pos.size`を使用して、エラー範囲全体にわたる^マーカーを表示するように、エラーの書式を改善

#### 0.2.2

- 追加
  - `examples`ディレクトリ
  - コンパイル時のスタックアロケーション機能
- 変更
  - ~~組み込み関数はユーザー定義関数より優先される~~
  - ~~ユーザー定義関数を優先するには `["$", "function"]` と記述する~~
  - `-` が引数 1 つを渡された場合、符号を反転させる

#### 0.2.1

- 追加
  - スコープの開始時にスタックを確保し、終了時にスタックを解放する機能

- 変更
  - ローカル変数がスタックに格納されるようになりました

- 修正
  - 32 ビット以上のリテラルを `+`, `-`, `*` が受け取るの場合にアセンブラが失敗する問題を修正

#### 0.2.0

- 追加
  - 新しい関数： ~~`eval`~~、`list`、~~`'`~~

- 変更
  - `begin` -> `scope`

### jsonpiler 0.1

#### 0.1.11

- 追加
  - 文字列リテラルのキャッシュと再利用

- 変更
  - アセンブリ・コードの変数生成を一般化
  - 内部構造体の名前

#### 0.1.10

- 修正
  - 前バージョンからのパーサーの不具合

#### 0.1.9

- 追加
  - スタック割り当てを動的に変更する機能

- 変更
  - エラーメッセージのフォーマット

- 修正
  - ~~パーサーのオーバーヘッドを削減~~

#### 0.1.8

- 追加
  - 関数 `global`

- 変更
  - 変数テーブルの構造
  - ~~`begin` はスコープを導入する~~

#### 0.1.7

- 追加
  - 自動保存とリストアによる一時的なレジスタ保存

- 変更
  - ~~メイン関数は  `ExitCode` を返す~~
  - 内部関数は必要なときだけバイナリに含まれる

#### 0.1.6

- 変更
  - 関数 `=` は `null` を返す
  - `$` はより多くの型をサポート

- 修正
  - docs.rs のドキュメントのビルドエラー

#### 0.1.5

- 追加
  - `CHANGELOG.md`

#### 0.1.4

- 修正
  - 以前のクレートのタイプミス

#### 0.1.3

- 変更
  - オブジェクトのエントリは挿入順を保持する
  - オブジェクト値の評価順序は挿入順

#### 0.1.2

- 追加
  - `README.md` 内の Mermaid 図

- 修正
  - 式の評価順序に影響するバグ

#### 0.1.0 ~ 0.1.1

- 以前のクレートから移行。
