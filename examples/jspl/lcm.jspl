define(
  gcd, { a: Int b: Int }, Int,
  {
    while(
      $b != 0,
      {
        temp = $b
        b = { $a % $b }
        a = $temp
      }
    )
    $a
  }
)
define(
  lcm, { a: Int b: Int }, Int,
  { $a * $b } / gcd($a, $b)
)
print("This program computes lcm(12, 18) = 36.\n")
lcm(12, 18)
